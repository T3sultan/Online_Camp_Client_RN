import { StyleSheet, Text, View } from "react-native";
import React from "react";

const BookMarkDetails = ({ navigation }) => {
  return (
    <View>
      <Text>BookMarkDetails</Text>
    </View>
  );
};

export default BookMarkDetails;

const styles = StyleSheet.create({});
